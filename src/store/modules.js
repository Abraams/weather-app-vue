import historyModule from '@/modules/History/store'
import searchModule from '@/modules/Search/store'
import weatherModule from '@/modules/Weather/store'

const modules = {
  history: historyModule,
  search: searchModule,
  weather: weatherModule
}

export default modules
